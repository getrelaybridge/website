---
const pathname = Astro.url.pathname;
const navItems = [
  { href: '/how-it-works/', label: 'How it works' },
  { href: '/security/', label: 'Security' },
  { href: '/guides/', label: 'Guides' },
  { href: '/docs/', label: 'Docs' },
];

const isCurrent = (href: string) => pathname === href || pathname.startsWith(href);
---

<header class="site-header">
  <div class="container nav-shell">
    <a class="wordmark" href="/" aria-label="RelayBridge home">
      <span class="brand-mark" aria-hidden="true"><i></i><i></i></span>
      <span>RelayBridge</span>
    </a>
    <nav aria-label="Primary navigation">
      <details class="mobile-nav">
        <summary>Menu</summary>
        <div class="mobile-nav-panel">
          {navItems.map((item) => (
            <a href={item.href} aria-current={isCurrent(item.href) ? 'page' : undefined}>{item.label}</a>
          ))}
          <a href="https://github.com/getrelaybridge/relaybridge">GitHub</a>
          <a href="/download/" aria-current={pathname === '/download/' ? 'page' : undefined}>Release status</a>
        </div>
      </details>
      <div class="desktop-nav">
        {navItems.map((item) => (
          <a href={item.href} aria-current={isCurrent(item.href) ? 'page' : undefined}>{item.label}</a>
        ))}
        <a href="https://github.com/getrelaybridge/relaybridge">GitHub</a>
        <a class="button button-small button-dark" href="/download/" aria-current={pathname === '/download/' ? 'page' : undefined}>Release status</a>
      </div>
    </nav>
  </div>
</header>
